#tuplas y listas

#estructura de datos con dif espacios de memoria pero que siempre referencia el primero
'''datos = ("hola", 2.5, 3, False)
print(len(datos))

#cuando llamo al index en python puedo llamar a [-1] y arranca de atrás hacia delante
print(datos[0])
print(datos[1])
print(datos[-1])

#si pongo rangos, no toma el último valor, es excluyente
print(datos[0:3]) 
#para pedir que muestre todos los datos sin conocer su largo
print(datos[0:-1]) 
#duplico el valor de la lista
print(datos*2)
#uso enumerate porque si pongo solo datos, lo tomaría solo como valor y no su index. Con enumerate armamos una matriz

for index, value in enumerate(datos):
    print(index, " -> ", value)'''

